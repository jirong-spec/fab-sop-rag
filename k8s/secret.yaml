# Template — do NOT commit real values.
# Generate with:
#   kubectl create secret generic fab-sop-secret -n fab-sop \
#     --from-literal=NEO4J_PASSWORD=<password> \
#     --from-literal=API_KEY=<api_key> \
#     --dry-run=client -o yaml > k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: fab-sop-secret
  namespace: fab-sop
type: Opaque
stringData:
  NEO4J_PASSWORD: "REPLACE_ME"
  API_KEY: "REPLACE_ME"
